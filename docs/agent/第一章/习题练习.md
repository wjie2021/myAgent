1、 选择D， A仅仅是一个硬件，不具备智能。B、类似于一个写死的工作流，比如 if（障碍物） 刹车，但是识别障碍物可能用到深度学习算法，但是也不属于智能体范畴。C、这种应该算是穷举后续的下棋路径，选项中的 规划 其实并不是真正的规划。 D、这是一个明细的智能体，需要进行感知、思考、规划、行动

2、人工智能领域，通常使用PEAS 模型来精确描述一个任务环境，即分析其性能度量(Performance)、环境(Environment)、执行器(Actuators)和传感器(Sensors）

P：支持按照用户的目标给出合理的计划、支持根据实时监测数据进行动作指导、支持按照训练效果进行饮食建议。
E：运动的规范性动作相关资料、健身目标与计划相关资料、健康饮食相关资料/
A：调用对应资料进行解析、展示到用户界面
S：检测用户生理数据、体态等硬件；调用资料接口进行解析。

特性:随机性：用户的生理数据、训练时的动作、甚至穿戴设备的是否为本人都是随机的。动态性：需要根据用户的生理数据实时进行监测和给出建议。 部分可观察：无论是计划、还是实时训练、都是一个动态的过程，而不是一个可预测的内容。


3.1: 
workflow: 这是一个死板的流程，但是对于电商这类的与金额、客户、商品核心内容有关的业务都应该是一个严谨的流程，所以这类审批流使用workflow是合理并且严谨的，缺点就是 对于审批流程来说，可能对客服或者主管来说经常会有重复的工作，并且7天内的商品退货为国家正常、所以基本上7天内都是会审批通过的，而审批不通过的情况较少，而且由于重复的机械操作，可能忽略的真正的需要审批不通过的商品。
agent：对于电商的核心业务如果完全交给agent，可能会存在风险，无论是决策是否退货，理解退款政策，评估商品状况可能会有偏差，一旦出错可能会带来损失，agent作为一个智能体并不能为损失负责，然而优点就是可以减少很多机械的重复性操作，并且分析、理解这些内容都是ai的强项。

3.2：我更倾向与agent和workflow并行的规则。如果agent操作一些由风险的内容，需要由人工确认。而这些风险内容则需要一个规范的政策交给这一个整体系统。

3.3 我感觉我3.2已经回复了


题目4：
1、记忆功能的实现：目前此助手暂时不支持手动输入，可以修改支持手动输入文本，此时需要给系统提示词加上一些限制，防止用户与LLM沟通的内容与旅行无关，比如提示当前助手仅支持旅游推荐，不支持其他问题哦！ 记忆的实现可以考虑每当检测到任务完成时，可以主动询问是否满意本次推荐，如果用户反馈满意则将最终的结论进行记录，可以精简记录后续用户再询问类似问题是可以加到上下文中，可以单独给系统开辟一些空间记录这些记忆内容。
2、在调用获取景点门票工具被反馈不足时，可以让智能体进入一开始的循环，但是需要将该景点的门票售罄的情况加到上下文中，再次进行推荐。
3、暂时没有好的思路，可以考虑将用户拒绝也添加到记忆中。